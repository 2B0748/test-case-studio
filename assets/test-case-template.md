# 测试用例模板（Markdown）

> 复制本表头开始编写。通用字段必填；类型专属字段按需追加列。
> 多类型混排时，按 功能 → 接口 → 性能 → 安全 → 兼容 分表。
> 单元格内多行内容（如多条步骤）用 <br> 分隔，经 md_to_xlsx.py 导出 Excel 时自动转为单元格内换行。

## 功能用例

| 用例编号 | 模块 | 子模块 | 用例标题 | 用例类型 | 设计方法 | 优先级 | 前置条件 | 测试步骤 | 测试数据 | 预期结果 | 需求追溯 |
|----------|------|--------|----------|----------|----------|--------|----------|----------|----------|----------|----------|
| LOGIN-001 | 登录 | 密码登录 | 【功能】【正常】正确密码登录成功 | 功能 | 等价类 | P0 | 用户已注册且未登录 | 1.打开登录页<br>2.输入账号<br>3.输入密码<br>4.点击登录 | 账号:test@x.com 密码:Pass@123 | 跳转首页，右上角显示用户名 | REQ-LOGIN-02 |

## 接口用例
> 在通用字段后追加：请求方法/URL、请求头、请求体、预期状态码、响应校验点

| 用例编号 | 模块 | 用例标题 | 优先级 | 请求方法/URL | 请求头 | 请求体 | 预期状态码 | 响应校验点 | 需求追溯 |
|----------|------|----------|--------|--------------|--------|--------|------------|------------|----------|
| API-LOGIN-001 | 登录 | 【接口】【正常】登录返回 token | P0 | POST /api/login | Content-Type:application/json | {"user":"test@x.com","pwd":"Pass@123"} | 200 | 返回 token 字段且非 null，user 信息正确 | REQ-LOGIN-02 |

## 性能用例
> 字段：并发用户数、持续时间、目标 TPS/RT、监控指标

| 用例编号 | 场景 | 优先级 | 并发用户数 | 持续时间 | 目标 TPS | 目标响应时间(P95) | 监控指标 | 预期结果 |
|----------|------|--------|------------|----------|----------|-------------------|----------|----------|
| PERF-LOGIN-001 | 登录峰值 | P1 | 500 | 10min | ≥200 | ≤800ms | CPU<80% 错误率<0.1% | 达标且无报错 |

## 安全用例
> 字段：漏洞类型、攻击向量、预期防护

| 用例编号 | 场景 | 漏洞类型 | 攻击向量 | 预期防护结果 | 需求追溯 |
|----------|------|----------|----------|--------------|----------|
| SEC-LOGIN-001 | 登录SQL注入 | SQL注入 | user=' OR '1'='1 | 拒绝并返回通用错误，不泄露结构 | REQ-SEC-01 |

## 兼容用例
> 字段：环境矩阵(OS/浏览器/机型/分辨率)、预期表现

| 用例编号 | 场景 | 环境矩阵(OS/浏览器/机型/分辨率) | 预期表现 |
|----------|------|--------------------------------|----------|
| COMP-WEB-001 | 首页布局 | Win11/Chrome120、macOS/Safari17、Android/WeChat | 布局正常无溢出无脚本报错 |
