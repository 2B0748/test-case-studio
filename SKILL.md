---
name: test-case-studio
description: "测试用例设计、编写与评审一体化 skill。当用户需要写测试用例、设计用例、拆解测试点、评审用例、检查用例覆盖度、接口用例、性能用例、安全用例、兼容性用例，或在上传 PRD、需求文档、用户故事、接口文档、原型后要求产出测试用例时使用。覆盖功能、接口(API)、性能、安全、兼容性全类型，支持 Markdown 与 Excel 双格式输出。"
agent_created: true
---

# 测试用例设计 · 编写 · 评审 Studio

一套把"需求 → 测试点 → 用例 → 评审"串成标准动作的工作流。
面向软件测试工程师，强调**覆盖度、可执行性、可追溯性**三大质量属性。

本文件是直接执行指令：skill 触发后，由你（智能体）按下文流程直接完成工作，不要把执行指令当成提示词再输出给用户。

## 何时使用

- 收到 PRD / 需求文档 / 用户故事 / 接口文档，需要产出测试用例
- 用户说"帮我写测试用例""设计一下测试点""评审这份用例""覆盖度够不够"
- 需要接口、性能、安全、兼容性等非功能类型的专项用例
- 需要把用例导出成 Excel / CSV 以便导入禅道、TestRail、Jira

## 执行模式（先判断）

- **默认模式 · 直接执行**：按四阶段工作流产出测试点清单、用例文件、评审报告。
- **导出模式 · 输出提示词**：仅当用户明确说"给我提示词""我要拿去别的平台/工具用"时，
  改为输出 `references/prompts.md` 中对应阶段的提示词模板，不直接执行。

## 输入来源（按优先级）

1. 用户引用/上传的文件 → 用 Read 工具按绝对路径读取（PRD、接口文档、原型说明等）
2. 用户在对话中粘贴的需求文本
3. 用户口头简述 → 关键信息缺失时（系统形态 B/S 还是 C/S、涉及模块、接口契约、
   目标环境）先澄清再动手

**禁止**在没有任何需求资料时凭空编造需求细节；也**不要**要求用户"粘贴 PRD"——
已有文件就直接读。

## 工作流（四阶段）

### 阶段1 · 需求理解与测试点拆解

只拆测试点，不写详细用例：
1. 提炼功能清单：按模块/子模块拆出所有可独立验证的功能点。
2. 识别业务规则与约束：字段规则、状态流转、权限、边界、依赖、异常分支。
3. 标注风险与优先级：标出核心主链路(P0)、易错点、历史缺陷高发区。
4. 形成测试点清单，每行：模块 / 测试点 / 类型(功能·接口·性能·安全·兼容) / 优先级 / 风险说明。

测试点粒度 = "一个测试点对应一类验证意图"，不过粗、不细化到步骤。

**规模控制（重要）**：需求较大（模块 ≥3 或测试点预估 >30）时，先只输出测试点清单，
与用户确认范围和优先级后再进入阶段2，避免后续返工。

### 阶段2 · 测试设计（方法 + 用例展开）

**先 Read `references/methodology.md`**，再为每个测试点选择设计方法并展开：

- 输入有取值范围/格式约束 → 等价类划分 + 边界值分析
- 多条件组合逻辑 → 判定表 / 因果图
- 状态流转明显 → 状态迁移法
- 业务流程/用户旅程 → 场景法(基本流+备选流)
- 历史缺陷/经验 → 错误推测法
- 参数组合爆炸 → 正交试验 / 两两组合(pairwise)

生成要求：
1. 每个测试点至少覆盖：正向(正常通过) + 反向(异常/边界/非法输入)。
2. 高风险 P0 测试点必须补充边界值与异常用例。
3. 每条用例标注所用"设计方法"，便于评审追溯。
4. 类型专项：接口类覆盖状态码/错误码/超时/幂等/契约；性能类覆盖并发/思考时间/
   目标 TPS/RT/资源水位；安全类覆盖注入/XSS/越权/认证绕过/敏感数据；兼容类覆盖
   OS/浏览器/机型/分辨率/版本矩阵。

### 阶段3 · 用例编写（格式输出）

通用字段（必填）：用例编号 | 模块 | 子模块 | 用例标题 | 用例类型 | 设计方法 | 优先级 |
前置条件 | 测试步骤 | 测试数据 | 预期结果 | 需求追溯

类型专属补充字段：
- 接口(API)：请求方法/URL、请求头、请求体、预期状态码、响应校验点
- 性能：并发用户数、持续时间、目标 TPS/响应时间、监控指标
- 安全：漏洞类型、攻击向量、预期防护结果
- 兼容：环境矩阵(OS/浏览器/机型/分辨率/版本)、预期表现

写法规范：
- 标题："【类型】【预期】动作+对象"，如"【功能】【正常】用户使用正确密码登录成功"。
- 步骤：每步一个动作、可操作；预期结果可判定、可观测，禁止"显示正确"类描述。
- 表头使用 `assets/test-case-template.md`；多类型混合时按
  "功能 → 接口 → 性能 → 安全 → 兼容"分表。
- 单元格内多行内容（如多条步骤）用 `<br>` 分隔，导出 Excel 时会自动转为单元格内换行。

**规模控制**：单批输出 ≤30 条用例；超出时按模块分批，每批之间向用户确认是否继续。

### 阶段4 · 用例评审（自检 + 清单）

**先 Read `references/review-checklist.md`**，按其中八维度逐条核对：
覆盖度 / 正确性 / 可执行性 / 独立性 / 颗粒度 / 可追溯 / 优先级 / 去重。

输出评审报告：评审结论(通过/有条件通过/打回) → 问题清单(含严重度与修订建议) →
覆盖度统计(需求覆盖率 X/Y、缺失/薄弱测试点) → 修订后完整用例表。
判定口径严格按 checklist 的"评审结论判定"执行。

用户只要求"评审"而自带用例时，直接读用户文件进入本阶段。

## 通用约定

- 默认输出 Markdown；用户要 Excel 时按下文"Excel / CSV 导出"流程转换。
- 每个用例必须带 `需求追溯` 字段。需求文档无 ID 时，用"章节号+要点"定位
  （如 PRD-3.2-登录），保证可回溯。
- 优先级用 P0(核心主链路)/P1(重要)/P2(一般)/P3(边缘) 四级；单模块 P0 建议 ≤20%。
- 用例编号全局唯一：模块前缀+序号（如 LOGIN-001、API-LOGIN-001）。

## 路径约定

- 本文件中所有相对路径（`references/`、`assets/`、`scripts/`）均基于
  **本 SKILL.md 所在目录**（skill 根目录）解析；读取或执行时一律拼成绝对路径。
- 产物文件（用例 md、xlsx、评审报告）写入当前工作区的 `outputs/` 目录。

## Excel / CSV 导出

1. 先把 Markdown 用例表写入文件，如 `outputs/xxx-测试用例.md`。
2. 运行转换脚本（使用可用的 Python 解释器，脚本零依赖可回退 CSV）：
   `python <skill根目录>/scripts/md_to_xlsx.py outputs/xxx-测试用例.md --out outputs/xxx-测试用例.xlsx`
3. 已安装 openpyxl 时产出真正的 .xlsx（按 `## ` 章节分 sheet）；未安装会自动回退
   为每章节一个 CSV（utf-8-sig，Excel 可直接打开）。
4. 用户只要 CSV 时加 `--csv`。

## 产物交付

- 生成任何文件后，必须调用 present_files 交付给用户：xlsx 与其 md 源文件一起给出。
- 纯对话内的测试点清单/评审结论可直接回复，不必落文件；但完整用例集建议落盘交付。

## 资源

- `references/methodology.md` — 测试设计方法库 + 各类型字段定义 + 优先级标准（阶段2前必读）
- `references/review-checklist.md` — 用例评审检查单（八维度 / 严重度 / 结论判定）（阶段4前必读）
- `references/prompts.md` — 可导出到外部工具的提示词模板（仅导出模式使用）
- `assets/test-case-template.md` — Markdown 用例模板（表头 + 示例）
- `assets/test-case-template.xlsx` — Excel 用例模板，供人工填写参照及导入禅道/TestRail 时对照列结构
- `scripts/md_to_xlsx.py` — Markdown 用例表 → Excel/CSV 转换器
